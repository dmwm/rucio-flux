apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../sources
  - cm-int-prometheus.yaml
  - ../prometheus
  - ./statsd
  - ../statsd
  - cm-int-eagle.yaml
  - ../kube-eagle
  # - cm-int-fluentbit.yaml #integration specific configurations
  - ../fluentbit #base fluent bit config

#configMapGenerator:
#  - name: flux-rucio-server
#    files:
#      - values.yaml=flux-rucio-server.yaml
#configurations:
#  - kustomizeconfig.yaml
